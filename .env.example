# =============================================================================
# RoteiroBR — variáveis de ambiente
# Copie este arquivo: cp .env.example .env
# =============================================================================

PORT=3000
NODE_ENV=development

# OpenStreetMap (opcionais — padrões = demos públicos)
PHOTON_URL=https://photon.komoot.io
OSRM_URL=https://router.project-osrm.org
# Aceita vários mirrors separados por vírgula
OVERPASS_URL=https://overpass-api.de/api/interpreter,https://overpass.kumi.systems/api/interpreter
